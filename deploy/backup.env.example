# Install after approval as /etc/fin2/backup.env (root, mode 600).
# Full fingerprint of an encryption public key; never store its private key here.
FIN2_BACKUP_RECIPIENT=REPLACE_WITH_PUBLIC_KEY_FINGERPRINT
