# Django configuration

`settings.py`, `urls.py`, and `wsgi.py` run the read-only dashboard under `/fin2/`. Authentication, admin, sessions, and ORM databases are absent; CSRF remains enabled. See [run instructions and configuration](../docs/dashboard.md).
